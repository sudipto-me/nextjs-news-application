export default function NewsNotFoundPage() {
    return (
        <div id="error">
            <h1>News not found</h1>
            <p>Unfortunately, the news has not been founded.</p>
        </div>
    );
}